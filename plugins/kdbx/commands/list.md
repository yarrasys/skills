---
description: List kdbx entry paths and configured environments (never values)
argument-hint: [group]
---
Use the kdbx skill to list entry paths (`kdbx list $ARGUMENTS`) and the
configured environments (`kdbx envs`), marking the active one. Never display
secret values — paths and variable names only.
